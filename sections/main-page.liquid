<div class="container">
  <div class="page-spacer">
    <div class="page">
      <h1 class="tp-display-5 text-center">{{ page.title }}</h1>

      {%- if page.content != empty -%}
        <div class="prose tp-body">
          {{ page.content }}
        </div>
      {%- endif -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Page",
  "class": "shopify-section--main-page",
  "tag": "section"
}
{% endschema %}